# local config (contains real SPO site URL -- not committed)
config.json

# browser profiles created by broker / chat
.edgeprofile/
.edgeprofile-chat/

# measurement output
logs/*.csv

# local corp segment cache (contains corp content in plaintext -- never commit)
knowledge/corp-cache.tsv
